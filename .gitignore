__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
.DS_Store

# Model checkpoints are not committed to the repo — they're downloaded on
# demand via `geoint-insight setup` (see _setup.py). Keep the folder itself
# out of git entirely.
checkpoints/
